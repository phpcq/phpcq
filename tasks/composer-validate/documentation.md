CCABS composer-validate
=======================

This task executes `composer validate`.

Utilized properties
-------------------
* `ccabs.bin.composer` the path to the composer executable (default: ${ccabs.bin.dir}/vendor/bin/composer or via environment $PATH).
