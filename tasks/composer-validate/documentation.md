PHPCQ composer-validate
=======================

This task executes `composer validate`.

Utilized properties
-------------------
* `phpcq.bin.composer` the path to the composer executable (default: ${phpcq.bin.dir}/composer or via environment $PATH).
