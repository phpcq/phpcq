phpcq:
  # ProjectConfigInterface contains this
  directories:
    - src
    - tests
  tools:
    author-validation:
      version: ^1.0
    phpcpd:
      version: ^2.0

author-validation:
  directories:
    tests: false
    examples:

# Example merge
#author-validation:
#  directories:
#    src:
#    examples:

phpcpd:
  customflags: #flags for all directories
  directories:
    a: # no special flags
    b:
    xyz:
      excluded:
        - ... a (string)
        - ... b (string)
      customflags: # flags for xyz only
